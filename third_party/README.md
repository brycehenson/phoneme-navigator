# Third-Party Code

Drop vendored dependencies or git submodules in this directory whenever a project needs to pin external tooling. The template ships empty so you can decide whether to use it at all.
